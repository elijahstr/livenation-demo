# Managed harness plan view

Run `bun build.ts` from this directory to regenerate `managed-harness-setup.html`.

The renderer uses Bun and native system fonts only. It reads the Markdown plan as its sole prose source.
