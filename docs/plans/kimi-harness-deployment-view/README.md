# Kimi harness deployment plan view

Run `bun build.ts` from this directory to regenerate `kimi-harness-deployment.html`.

The renderer projects its prose from the Markdown plan. The generated HTML uses only inline CSS and native system fonts.
